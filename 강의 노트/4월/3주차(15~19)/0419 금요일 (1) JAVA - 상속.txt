강의내용1.txt
상속
1. 상속이란?
2. 클래스의 상속
class 자식클래스 extends 부모 클래스{

}
3. 클래스 상속 문법
5. super
	지역 변수 
		상위 클래스의 객체의 주소값 
		
	참고 ) this : 현재 클래스 객체의 주소값
	함수(메서드)
		상위 클래스의 생성자 함수를 클래스 내부에서 호출 할 떄 사용
	
	참고 ) this(..): 현재클래스의 생성자 함수를 클래스 내부에서 호출 할 때 사용
	
C()->super():B()->super A()

하위 클래스의 객체 -> 상위 클래스의 자료형으로 자동 형변환

instance: 실체 - 실제로 존재한다.
클래스라고 하는 객체의 정의 ( 존재 X) -> new -> 힙공간에 존재  : 생성된 객체
사실상 인스턴스랑 객체는 같음

instanceof 연산자 : 객체의 생성된 출처를 체크해보는 연산자 
c instanceof C: c라고 하는 참조 변수가 가리키는 객체의 출처가 c클래스로부터 만든 객체인가?

상위 클래스 : 일반적인 개념
하위 클래스 : 구체적인 개념

메서드 재정의
1. 메소드 재정의 : 상속시에만 가능 (객체가 될수 있는 인스턴스메서드에만 가능)	
	하위클래스에 상위클래스와 동일한 메서드가 있으면
	하위클래스의 메서드가 우선
	
	상속은 객체간에만 가능
2. 묵시적 형변환과 메서드 재정의
	애노테이션(Annotation): 주석 주해
	형식 : @이름
	정보 전달
	 
	@Override:  재정의된 메서드 임을 컴파일러에게 알려주는 정보
	
	재정의시 접근 제어자 변경 범위
	
	좁은 범위-> 넓은 범위 변경 가능 : private 은 접근 자체가 안되므로 불가
	
	넓은 범위 -> 좁은 범위 (x)
3. 가상메서드


다형성  
1. 다운 캐스팅과 instanceof